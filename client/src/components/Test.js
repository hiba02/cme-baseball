import React from "react";

const Test = () => {
  // const query = queryString.parse(location.search);
  // console.log(query);
  return (
    <div>
      THis is a Test Componet
      {/* <p>match.params.name: {match.params.name}</p> */}
    </div>
  );
};

export default Test;
